import sys
from PyQt6.QtWidgets import QMainWindow, QApplication

if __name__ == "__main__":
    app = QApplication(sys.argv)
    window = QMainWindow()
    window.resize(800, 600)
    window.setWindowTitle("Cửa số đầu tiên")
    window.show()

sys.exit(app.exec())